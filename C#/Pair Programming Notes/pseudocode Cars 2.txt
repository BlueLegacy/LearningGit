	public class main(){
	main{
		cars camaro = new cars();
		camaro.setYearModel("1992");
		camaro.setMake("Camaro RS");
		camaro.setSpeed(85);
	
			display(camaro.getYearModel()+" "+camaro.getMake());
			display("The speed for this vehicle is "+camaro.getSpeed());
		
		}
		
	}
		for( int i =0; i<5; i++)
		{
			camaro.accelerate();
			display (camaro.getSpeed());
		}
		
		for( int i =0; i<5; i++)
		{
			camaro.brake();
			display (camaro.getSpeed());
		}
		